using System.Windows.Controls;

namespace POS.Desktop.Views;

/// <summary>Modal de resultado (venta completada, vuelto, warnings, Nueva venta). Sin lógica.</summary>
public partial class ResultModalView : UserControl
{
    public ResultModalView() => InitializeComponent();
}