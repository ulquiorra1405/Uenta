<UserControl x:Class="POS.Desktop.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:POS.Desktop.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <UserControl.Resources>
        <!-- Chip de periodo del dashboard (igual patrón que MethodChip) -->
        <Style x:Key="PeriodChipBase" TargetType="Button">
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Padding" Value="14,7" />
            <Setter Property="MinHeight" Value="30" />
            <Setter Property="FontFamily" Value="{StaticResource BodyFont}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="FontSize" Value="12.5" />
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Chrome" Background="Transparent" CornerRadius="6"
                                BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="{StaticResource MutedBrush}" />
                                <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Chrome" Property="Opacity" Value="0.7" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PeriodChipToday" TargetType="Button" BasedOn="{StaticResource PeriodChipBase}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsPeriodToday}" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryDarkBrush}" />
                    <Setter Property="Foreground" Value="White" />
                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryDarkBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="PeriodChip7" TargetType="Button" BasedOn="{StaticResource PeriodChipBase}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsPeriod7Days}" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryDarkBrush}" />
                    <Setter Property="Foreground" Value="White" />
                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryDarkBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="PeriodChip30" TargetType="Button" BasedOn="{StaticResource PeriodChipBase}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsPeriod30Days}" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryDarkBrush}" />
                    <Setter Property="Foreground" Value="White" />
                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryDarkBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Encabezado -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Reportes" Style="{StaticResource SectionTitle}"
                           Margin="0,0,0,4" />
                <TextBlock Grid.Column="0" Text="Resumen de ventas del negocio" FontSize="12.5"
                           Foreground="{StaticResource TextSecondaryBrush}" Margin="0,30,0,0" />
            </Grid>

            <!-- Error de carga -->
            <Border Grid.Row="1" Background="{StaticResource DangerLightBrush}" CornerRadius="6" Padding="12,10"
                    Margin="0,10,0,0"
                    Visibility="{Binding LoadError, Converter={StaticResource NullToVis}}">
                <TextBlock Text="{Binding LoadError}" FontSize="12" Foreground="{StaticResource DangerBrush}"
                           TextWrapping="Wrap" />
            </Border>

            <!-- KPIs del día -->
            <Grid Grid.Row="2" Margin="0,16,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource CardBorder}" Padding="16">
                    <StackPanel>
                        <TextBlock Text="Ventas de hoy" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" />
                        <TextBlock Text="{Binding TodayTotalText}" FontSize="24" FontWeight="Bold"
                                   FontFamily="{StaticResource DisplayFont}" Margin="0,4,0,0" />
                    </StackPanel>
                </Border>
                <Border Grid.Column="2" Style="{StaticResource CardBorder}" Padding="16">
                    <StackPanel>
                        <TextBlock Text="Tickets de hoy" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" />
                        <TextBlock Text="{Binding TodayTicketsText}" FontSize="24" FontWeight="Bold"
                                   FontFamily="{StaticResource DisplayFont}" Margin="0,4,0,0" />
                    </StackPanel>
                </Border>
                <Border Grid.Column="4" Style="{StaticResource CardBorder}" Padding="16">
                    <StackPanel>
                        <TextBlock Text="Ticket promedio" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" />
                        <TextBlock Text="{Binding TodayAverageText}" FontSize="24" FontWeight="Bold"
                                   FontFamily="{StaticResource DisplayFont}" Margin="0,4,0,0" />
                    </StackPanel>
                </Border>
                <Border Grid.Column="6" Style="{StaticResource CardBorder}" Padding="16">
                    <StackPanel>
                        <TextBlock Text="Ítems vendidos" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" />
                        <TextBlock Text="{Binding TodayItemsText}" FontSize="24" FontWeight="Bold"
                                   FontFamily="{StaticResource DisplayFont}" Margin="0,4,0,0" />
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Selector de periodo + tablas -->
            <Grid Grid.Row="3" Margin="0,16,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- Selector de periodo -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                    <TextBlock Text="Periodo: " FontSize="12.5" VerticalAlignment="Center"
                               Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,6,0" />
                    <Button Content="Hoy" Style="{StaticResource PeriodChipToday}"
                            AutomationProperties.Name="PeriodoHoy"
                            Command="{Binding SelectPeriodCommand}" CommandParameter="today" />
                    <Button Content="7 días" Style="{StaticResource PeriodChip7}" Margin="6,0,0,0"
                            AutomationProperties.Name="Periodo7Dias"
                            Command="{Binding SelectPeriodCommand}" CommandParameter="7" />
                    <Button Content="30 días" Style="{StaticResource PeriodChip30}" Margin="6,0,0,0"
                            AutomationProperties.Name="Periodo30Dias"
                            Command="{Binding SelectPeriodCommand}" CommandParameter="30" />
                </StackPanel>

                <!-- Dos tablas: top productos + ventas por cajero -->
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="12" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!-- Top 5 productos -->
                    <Border Grid.Column="0" Style="{StaticResource CardBorder}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <StackPanel Grid.Row="0" Margin="14,12,14,8">
                                <TextBlock Text="Productos más vendidos" FontSize="13.5" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding PeriodLabel}" FontSize="11.5"
                                           Foreground="{StaticResource TextSecondaryBrush}" Margin="0,2,0,0" />
                            </StackPanel>
                            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                                <ItemsControl ItemsSource="{Binding TopProducts}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate DataType="{x:Type vm:TopProductListItem}">
                                            <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1"
                                                    Padding="14,10">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="Auto" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="0" Text="{Binding ProductName}"
                                                               FontSize="13" FontWeight="SemiBold"
                                                               TextTrimming="CharacterEllipsis" />
                                                    <TextBlock Grid.Column="1" Text="{Binding QuantityText}"
                                                               FontSize="12.5" Foreground="{StaticResource TextSecondaryBrush}"
                                                               Margin="12,0" VerticalAlignment="Center" />
                                                    <TextBlock Grid.Column="2" Text="{Binding TotalText}"
                                                               FontSize="13" FontWeight="SemiBold"
                                                               VerticalAlignment="Center" />
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Grid>
                    </Border>

                    <!-- Ventas por cajero -->
                    <Border Grid.Column="2" Style="{StaticResource CardBorder}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <StackPanel Grid.Row="0" Margin="14,12,14,8">
                                <TextBlock Text="Ventas por vendedor" FontSize="13.5" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding PeriodLabel}" FontSize="11.5"
                                           Foreground="{StaticResource TextSecondaryBrush}" Margin="0,2,0,0" />
                            </StackPanel>
                            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                                <ItemsControl ItemsSource="{Binding SalesByUser}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate DataType="{x:Type vm:SalesByUserListItem}">
                                            <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1"
                                                    Padding="14,10">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="Auto" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="0" Text="{Binding UserName}"
                                                               FontSize="13" FontWeight="SemiBold" />
                                                    <TextBlock Grid.Column="1" Text="{Binding TicketText}"
                                                               FontSize="12.5" Foreground="{StaticResource TextSecondaryBrush}"
                                                               Margin="12,0" VerticalAlignment="Center" />
                                                    <TextBlock Grid.Column="2" Text="{Binding TotalText}"
                                                               FontSize="13" FontWeight="SemiBold"
                                                               VerticalAlignment="Center" />
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Grid>
                    </Border>
                </Grid>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>