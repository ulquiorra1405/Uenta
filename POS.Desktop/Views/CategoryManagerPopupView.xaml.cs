using System.Windows.Controls;

namespace POS.Desktop.Views;

/// <summary>Overlay global: gestor de categorías (crear + renombrar inline). Sin lógica.</summary>
public partial class CategoryManagerPopupView : UserControl
{
    public CategoryManagerPopupView() => InitializeComponent();
}
